# Volume Control
"amixer -c 1 sset Master 3-"
    Mod2 + XF86AudioLowerVolume
"amixer -c 1 sset Master 3+"
    Mod2 + XF86AudioRaiseVolume

# Lock screen, requires bin/lock
"lock"
    m:0x50 + c:24

# Generate random UUID, requires bin/uuid
"uuid"
    F12
